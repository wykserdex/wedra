# my_summarizer

TODO: одна строка — что делает плагин, что читает, что пишет.

## Цикл разработки

```bash
# правим main.py, затем:
tool plugin test my_summarizer      # контракт-тесты (зелёные из коробки)
tool plugin validate my_summarizer  # проверка манифеста
```

Протокол и правила контракта: PROTOCOL.md в корне репозитория.
