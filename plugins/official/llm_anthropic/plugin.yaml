id: llm_anthropic
version: 0.1.0
platform_api: "^0.1"

runtime:
  type: python
  entry: main.py
  requires: []          # только stdlib

input:
  prompt:
    from: input.topic
    type: string
    format: text
  system:
    from: input.draft_system
    type: string
    optional: true

output:
  text:  { type: string, format: text }
  model: { type: string }

permissions:
  network: [ { host: "api.anthropic.com", port: 443 } ]
  filesystem: none
  secrets: [ "ANTHROPIC_API_KEY" ]
