# Волна 2 — «напиши плагин, попади в реестр» (v0.17)

**Смена механики vs M5.** В M5 просили тестировать (фидбек = успех). Теперь
просим **написать плагин** — и у просьбы появился видимый результат: плагин
попадает в публичный реестр (`registry.yaml`), любой делает
`orchestrator pipeline install <пресет>` — и получает его автоматически.
Имя автора — в манифесте, конформность — подтверждена CI, не «на честном слове».

Публичную рассылку (0 звёзд на всех репах) — **нет**. Волна только по
знакомым/полузнакомым, персонально, по 1–2 человека в день.

---

## Аудитория (по M5_FEEDBACK.md)

| Кто | Состояние | Хук |
|---|---|---|
| **Тестер №1** (тёплый) | писал `text_analyzer`, переписал `dir_lister` без `from`; **открытый вопрос M5: «прислать оригинал dir_lister для замены реконструкции»** | «твой dir_lister в репо — моя реконструкция по твоим отчётам, ждём оригинал» + новый плагин |
| **Автор №4** (тёплый) | `csv_loader` + `email_triage` уже в репо | «ваши два плагина теперь в реестре — one-command install; добавьте ещё один (меню идей)» |
| **Тестер №3** (тёплый, нулевой цикл) | описал OSINT-пайплайн нормализации/свидания идентиков | «ваш entity-пайплайн идеален как первый внешний пресет — нужен плагин `entity_normalizer`» |
| **Холодные devs** (2–3 DM) | не видели проект | короткое сообщение + menu идей + бинарник из Release |

---

## Сообщения (копипаст, править под человека)

### Тестер №1 (тёплое)

> Слушай, у нас v0.17: из всего, что ты писал в фидбеке M5, главное —
> реестр и CI-конформность. Конкретно это значит:
> 1) твои text_analyzer и dir_lister теперь в публичном реестре — любой
>    `orchestrator plugin install text_analyzer` и у него рабочий плагин,
>    имя автора в манифесте;
> 2) dir_lister в репо — моя реконструкция по твоим отчётам (я помню, ты
>    обещал прислать оригинал) — скинь, поменяю, пусть будет твой;
> 3) запрос: один маленький плагин (15–40 минут, есть готовое ТЗ в
>    TUTORIAL_PLUGINS.md + меню идей). Пропишется в реестр, CI проверит
>    конформность. Скелет генератор делает сам: `orchestrator plugin create`.
>
> Репо: github.com/wykserdex/wedra, бинарник — в Releases (v0.17, все ОС).

### Автор №4 (тёплое)

> v0.17: csv_loader и email_triage, которые ты писал на M5, теперь в
> публичном реестре — `orchestrator pipeline install email_check` тянет
> твои плагины автоматически к любому, кто берёт пресет.
> Хочешь ещё один? 15–40 минут, скелет сам генерируется, есть меню идей
> (json_flatten, phone_normalizer, url_metadata — чистая логика без сети и
> ключей). Пропишется в реестр с твоим именем, CI подтвердит конформность.
>
> Репо: github.com/wykserdex/wedra, бинарник — Releases v0.17.

### Тестер №3 (тёплое, OSINT)

> Ваш нулевой цикл (нормализация → дедуп → слияние → confidence →
> corroboration) дошёл до реестра другой стороны: в v0.17 пресеты ставятся
> одной командой и тянут свои плагины. Ваш пайплайн — идеальный кандидат в
> первый внешний пресет. Не хватает одного плагина: `entity_normalizer`
> (username/email/phone → каноничные формы). ТЗ в одном абзаце, тесты
> без сети, 30 минут. Пропишется в реестр, author = вы.
>
> Репо: github.com/wykserdex/wedra, бинарник — Releases v0.17.

### Холодный dev (коротко)

> Пишу локальный оркестратор цепочек (CLI, Go; человек в петле, журналы,
> resume). Плагин = обычный python-скрипт с JSON по stdio, контракт на
> 15 минут чтения (TUTORIAL_PLUGINS.md). Сейчас набираю первые плагины в
> публичный реестр: `orchestrator plugin create` делает скелет, `plugin
> test` — конформность, всё это проверяет CI. Если интересно — 30–40 минут
> на один маленький плагин (json_flatten, date_parser, phone_normalizer —
> меню в репо). Репо: github.com/wykserdex/wedra, бинарник в Releases.

---

## Меню идей (без сети, без ключей, всё тестируется офлайн)

| id | Что делает | вход → выход |
|---|---|---|
| `date_parser` | человекочитаемые даты → ISO 8601 (ru/en, частые форматы) | string → string(iso) |
| `phone_normalizer` | любой формат → E.164 (+375 (29) 123-45-67 → +375291234567) | string → string(e164) |
| `regex_extract` | все совпадения паттерна в тексте | text + pattern → array + count |
| `url_metadata` | url → domain/tld/path/query (чистый парсинг, без сети) | string → 4 поля |
| `json_validate` | JSON-строка против списка обязательных ключей | string(json) + keys → ok/mismatch |
| `text_dedup` | дедуп строк + счётчики | string → array + count |
| `time_diff` | длительность между двумя ISO-датами | 2×string(iso) → string + number(sec) |
| `slugify` | человекочитаемая строка → url-safe slug | string → string(slug) |
| `markdown_links` | все ссылки из markdown/текста | string → array + count |
| `json_flatten` | JSON → плоский map (точки в ключах) | string(json) → string(json) |
| `entity_normalizer` | username/email/phone → каноничные формы (резерв — домен Тестера №3) | string → object |

Правила каждого: happy path + доменная ошибка (когда не распарсить) +
битый JSON (exit 2). Манифест: `permissions: network: []`, `secrets: []` —
честно, раз сети и ключей нет.

**Раздача по сетам (во избежание столкновений, 2 автора × 2–3 плагина):**

- **Сет A**: date_parser, phone_normalizer, regex_extract
- **Сет B**: url_metadata, json_validate, text_dedup
- **Резерв** (для Тестера №3 / следующих): entity_normalizer, time_diff,
  slugify, markdown_links, json_flatten

Каждый автор берёт 2–3 СВОИХ сета; своё тоже можно, если есть идея —
главное, чтобы не пересекалось со вторым (пересеклось — скажу, поменяем).

## Как принимаем (трение по возрастанию — автору любой вариант ок)

1. **«скинь папку»** — автор работает в клоне wedra, `plugin test` зелёный,
   присылает zip (или ссылку на свой каталог). Я делаю PR: каталог + запись
   в registry.yaml. Самый быстрый путь.
2. **Fork + PR** — если есть GitHub. Шаблон: `plugins/community/<name>/`
   + 4 строки в registry.yaml (source=wedra, path, version=тег релиза).
3. **Свой репо с тегом** — для «stock plugin в своём репо» (конечное
   состояние экосистемы): запись в registry.yaml указывает `source` на репо
   автора, `version` на его тег. Полный владелец плагина — автор.

## Целевая установка (не «фидбек», а результат)

- ≥ 2 новых плагина в реестре с чужим author.
- ≥ 1 из них использован третьим лицом через `pipeline install` (запросить
  у третьего: «вот пресет, поставь и скажи, что сломалось»).
- 0 ситуаций «не понял, что писать в permissions» — если случится, чиним
  TUTORIAL, а не автора.

## Приёмка батчей (что спрашивать у автора, чтобы не тянуть)

1. Где файлы: ссылка на Drive-папку / zip / гит-репо (gdown берёт Drive-папки).
2. Атрибуция: как записать в `author:` (ник/имя + канал, напр. «волна 2»).
3. Какие из меню взял + свои (чтобы проверить пересечения сразу).

Дальше я: конформность (`plugin validate` + `plugin test`), правки только в
тестах/манифестах (логику не трогаю, кроме явных багов), реестр, релиз-тег.

## Трекер раунда

| Дата | Кто | Плагин | Как принял (zip/PR/репо) | Конформность (ср. N правок) | В реестре | Использовали 3-м лицом |
|------|-----|--------|--------------------------|------------------------------|-----------|------------------------|
| 2026-08-30 | Тестер №1 (DM) | word_freq, json_diff, batch_email_triage, report_formatter + оригинал dir_lister | Google Drive (папка) | 3 правки при приёмке (format на number, expect crash→platform:bad_input, фикстуры в зипе) | ✓ v0.18 (атрибуция: community · тестер №1) | — (след. шаг: пресет) |
| 2026-08-30 | Тестер №2 (DM) | — (первый плагин: ТЗ отправлено, ждём выбор из меню) | — | — | — | — |

Замечания раунда: единственный дефект волны — бинарник v0.17 показывал
старую версию (api.Version const 0.14.1, файл VERSION рядом с бинарником
отсутствует). Исправлено: var + ldflags из тега; релиз пересобран на месте.
Все doby M5 у тестеров закрыты, --resume оценён положительно.

## Порядок (не 10 DM за вечер)

День 1: Тестер №1 + Автор №4 (тёплые, персональные). Ждём 24ч.
День 2: Тестер №3 (OSINT) + 1 холодный DM. Ждём 24ч.
День 3+: по ответам; холодных — не более 2 в день.
