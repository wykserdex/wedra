# Ожидание намеренно «мягкое» — тестируем, что FAIL даёт сам контракт типов,
# а не матчер.
tests:
  - name: вернёт значение, но не того типа
    input: {}
    expect:
      status: ok
      output:
        value: { present: true }
