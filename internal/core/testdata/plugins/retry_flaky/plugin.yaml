id: retry_flaky
version: 0.1.0
platform_api: "^0.1"

runtime:
  type: python
  entry: main.py

input: {}

output:
  result: { type: string }

permissions:
  filesystem: workspace  # пишет _counter рядом с собой (только для тестов)
  secrets: []
