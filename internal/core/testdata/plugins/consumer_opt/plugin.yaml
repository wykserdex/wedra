id: consumer_opt
version: 0.1.0
platform_api: "^0.1"

runtime:
  type: python
  entry: main.py

input:
  data: { from: steps.producer.result, type: string, optional: true }

output:
  got: { type: string }

permissions:
  filesystem: none
  secrets: []
