format_version: "0.2"
pipeline:
  name: bind_unknown_port
  input:
    email: "a@b.c"
  steps:
    - id: s
      plugin: fake/syntax
      bind:
        email: input.email
        nope_port: input.email
