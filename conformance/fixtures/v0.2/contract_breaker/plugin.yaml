id: contract_breaker
version: 0.1.0
platform_api: "^0.1"

runtime:
  type: python
  entry: main.py

input: {}

output:
  value: { type: string }   # обязательный, но плагин его не вернёт

permissions:
  filesystem: none
  secrets: []
