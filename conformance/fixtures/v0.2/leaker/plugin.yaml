id: leaker
version: 0.1.0
platform_api: "^0.1"

runtime:
  type: python
  entry: main.py

input: {}

output:
  value: { type: string }   # junk НЕ объявлен — должен быть отброшен с warning

permissions:
  filesystem: none
  secrets: []
